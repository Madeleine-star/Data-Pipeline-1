# Scénario03:
#Afficher les recettes avec des images
#Ce scénario XSLT prend un fichier XML contenant des données de recettes et le transforme en un fichier HTML qui affiche chaque recette avec une image associée.

<?xml version="1.0" encoding="UTF-8"?>
<xsl:stylesheet xmlns:xsl="http://www.w3.org/1999/XSL/Transform"
  xmlns:xs="http://www.w3.org/2001/XMLSchema"
  exclude-result-prefixes="xs"
  version="2.0">

  <xsl:output method="html" indent="yes"/>

  <xsl:template match="/">
    <html>
      <body>
        <h1>Recettes avec images</h1>
        <xsl:for-each select="//recette">
          <h2>
            <xsl:value-of select="nom"/>
          </h2>
          <img src="{image}" alt="{nom}"/>
          <p>
            <xsl:value-of select="instructions"/>
          </p>
        </xsl:for-each>
      </body>
    </html>
  </xsl:template>

</xsl:stylesheet>

